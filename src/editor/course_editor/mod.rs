use bevy::prelude::*;

pub struct CourseEditorPlugin;

impl Plugin for CourseEditorPlugin {
    fn build(&self, _app: &mut App) {
        // Course editor systems will be registered in Phase 5
    }
}
